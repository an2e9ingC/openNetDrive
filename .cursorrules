# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

openNetDrive 是一个跨平台的网络驱动器挂载工具，支持通过 WebDAV 和 SMB 协议将 NAS 共享文件夹映射为本地磁盘。采用 GPL-3.0 协议开源，支持后续商业化（双授权模式）。

## 技术栈

- **后端**: Rust 2021 Edition
- **前端**: Tauri v2 + React 18 + TypeScript
- **文件系统**: WinFsp (Windows) / macFUSE (macOS) / FUSE3 (Linux)
- **协议**: WebDAV (reqwest) / SMB (libsmb2-rs)

## 项目结构

```
openNetDrive/
├── Cargo.toml              # 工作空间配置
├── README.md
├── LICENSE                 # GPL-3.0
├── packages/
│   ├── core/               # 核心库
│   │   ├── src/
│   │   │   ├── lib.rs      # 导出模块
│   │   │   ├── config.rs   # 配置管理
│   │   │   ├── protocol.rs # 协议 trait
│   │   │   ├── credentials.rs  # 凭据管理
│   │   │   └── error.rs    # 错误类型
│   │   └── Cargo.toml
│   ├── mount-win/          # Windows 挂载 (WinFsp)
│   ├── mount-macos/        # macOS 挂载 (macFUSE)
│   ├── mount-linux/        # Linux 挂载 (FUSE3)
│   ├── cli/                # 命令行工具 (ond)
│   └── tauri-app/          # Tauri UI 应用
```

## 常用命令

```bash
# 构建所有包
cargo build

# 运行 CLI
cargo run -p opennetdrive-cli -- help
cargo run -p opennetdrive-cli -- list
cargo run -p opennetdrive-cli -- add -n "My NAS" -t webdav -h https://example.com/dav

# 开发模式运行 Tauri
cd packages/tauri-app
npm install
npm run tauri dev

# 打包发布
cargo build --release -p opennetdrive-cli
```

## 架构设计

1. **core 库**: 协议抽象 (trait)、配置管理、凭据存储
2. **mount-* 包**: 各平台的文件系统驱动实现
3. **cli**: 命令行配置和管理工具
4. **tauri-app**: 跨平台桌面 UI

## 开发注意事项

1. Windows 开发需要安装 WinFsp: https://winfsp.dev/
2. Tauri 开发需要 Rust 最新版和 Node.js 18+
3. 跨平台编译需要各平台的 FUSE 库
4. 凭据管理使用系统原生的 Credential Manager / Keychain / Secret Service
