{
  "permissions": {
    "allow": [
      "WebFetch",
      "WebSearch",
      "Bash"
    ],
    "deny": [
      "Bash(rm -rf .git)",
      "Bash(rm -rf .git/)",
      "Bash(rmdir .git)",
      "Bash(rmdir .git/)",
      "Bash(rm -r .git)",
      "Bash(rm -rv .git)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "command=$(jq -r '.tool_input.command' <<< '$ARGUMENTS'); if echo \"$command\" | grep -Eq 'rm.*(-[rfv]*\\s*)*\\.git|rmdir\\s+\\.git|rmdir\\s+\\.git/|rm\\s+-rf\\s+\\.git'; then echo '{\"continue\": false, \"stopReason\": \"禁止删除 .git 目录！这会丢失所有 git 历史记录。\"}'; exit 1; fi; echo '{\"continue\": true}'"
          }
        ]
      }
    ]
  }
}